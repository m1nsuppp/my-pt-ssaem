# 응답 

- 한국어로 답한다. 문장을 "~음/~함/~임/~됨" 등으로 끝낸다.
- 최단 정답 우선. 인사, 서두, 맺음말, 질문 재진술, 답 후 요약 금지.
- 표 > 문단, 코드 > 설명. 짧은 답엔 헤더 없이.

# 개발 마인드셋

- 인터페이스는 내부 구현이 아닌 내 코드를 사용하는 쪽의 관점에서 설계한다.
  - 메서드명과 시그니처만 보고 동작을 예측할 수 있어야 한다.
- 성급한 추상화를 피한다. 잘못된 추상화를 제공하느니 차라리 없는 것이 낫다. 
  - 중복으로 보여도, 우연적 중복인지 확인 후 올바른 추상화를 제공할 수 있도록 고민한다.
- 외부 의존성(SDK, API 클라이언트 등)은 자체 인터페이스로 래핑한다. 직접 import하여 의존하지 않는다.
- 모든 선언(모듈, 함수, 타입, 테스트 등)은 소비되는 맥락에서 가장 가까운 위치에 둔다. 
  - 여러 곳에서 공유되기 전까지 상위 경로로 올리지 않는다.
- 버그를 숨기지 않는다. 과감하게 error를 throw한다.
- mock/stub 대신 fake 구현을 선호한다.
- 테스트 코드를 작성하기 어렵다면 구현 구조를 먼저 의심한다.
- 테스트는 공개 인터페이스(입력 → 출력)만 검증한다. 내부 호출 순서나 구현 세부사항을 단언하지 않는다. 
  - 항상 사용자 중심으로 테스트 케이스를 작성한다.
- 시스템을 계층 단위로 애자일하게 확장할 수 있도록 사고한다.